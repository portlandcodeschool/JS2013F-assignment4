
var tots = 0;
var item = 1.25;
var takeout = 1.15;

function prep(number) {
    number *= 100
    return number
}

if(item % 1 === 0)
if(takeout % 1 === 0)

tots = tots + 1.25 
tots = tots - 1.5 

// tots = prep(tots) - prep(takeout);

// tots = prep(tots) + prep(item);

// function add (x) {
//       x = x.toFixed(2);
//       tots = tots.toFixed(2);
//       tots = tots+x;
//       console.log(tots);
//     };

// function subtract (x) {
//       x = x.toFixed(2);
//       tots = tots.toFixed(2);
//       tots = tots-x;
//       console.log(tots);
//     };

// console.log (add(item.toFixed))
// console.log (subtract(takeout))
console.log (tots)

// newCR.add(1.25);